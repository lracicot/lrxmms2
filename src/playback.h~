#ifndef __LRXMMS2_PLAYLIST_GTK_MODEL_H__
#define __LRXMMS2_PLAYLIST_GTK_MODEL_H__

void playback_play( GtkWidget *widget,
                        gpointer   connection );

void playback_pause( GtkWidget *widget,
                        gpointer   connection );

void playback_stop( GtkWidget *widget,
                        gpointer   connection );

#endif